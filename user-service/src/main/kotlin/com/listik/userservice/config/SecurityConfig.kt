package com.listik.userservice.config

import org.springframework.context.annotation.Configuration

@Configuration
class SecurityConfig {
    // SecurityConfig removed as authentication is now handled by auth-service
    // This service only manages user profiles and entities
}